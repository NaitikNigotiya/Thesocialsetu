# Workspace Rules for The Social Setu

## Git Commit Policy
- **Do NOT automatically commit or push to Git/GitHub after every task or edit.**
- Only run `git commit` or `git push` when the user explicitly requests to commit or push changes.
